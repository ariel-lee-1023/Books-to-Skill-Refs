# macOS / editor cruft
.DS_Store
._*
.idea/
.vscode/
*.swp

# Python (the extraction engine this skill calls is Python)
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
*.egg-info/

# Vendored copy of book-to-skill's extraction engine, if placed next to this skill.
# Only that project's package is ignored — /scripts/ is deliberately NOT listed, so a
# first-party extractor written here can be committed normally.
/book_to_skill/

# Source documents and generated libraries — never commit book contents
/input/
/books/
/output/
*.pdf
*.epub
*.mobi
*.azw
*.azw3
*.docx
*.rtf
